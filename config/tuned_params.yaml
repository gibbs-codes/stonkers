# Tuned parameters (Session 5A verdict: use 15-minute candles)
# Goal: tighten gates until profitable without choking signal flow

momentum_thrust:
  entry_threshold: 1.0
  volume_multiplier: 0.5

vwap_mean_rev:
  std_multiplier: 1.3
  volume_threshold: 0.5

support_resistance_breakout:
  volume_multiplier: 0.8
  retest_candles: 10
