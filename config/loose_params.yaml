# Loose parameters (Session 5A verdict: use 15-minute candles)
# Goal: maximize signal count so we can see behavior on fast feedback

momentum_thrust:
  entry_threshold: 0.5
  volume_multiplier: 0.3

vwap_mean_rev:
  std_multiplier: 1.0
  volume_threshold: 0.3

support_resistance_breakout:
  volume_multiplier: 0.5
  retest_candles: 15
